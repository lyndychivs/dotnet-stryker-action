name: shellcheck
run-name: ShellCheck entrypoint.sh

on:
  push:
  pull_request:

jobs:
  shellcheck:
    runs-on: ubuntu-latest

    permissions:
      contents: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v6

      - name: ShellCheck
        run: shellcheck entrypoint.sh
